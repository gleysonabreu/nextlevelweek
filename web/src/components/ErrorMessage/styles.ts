import styled from 'styled-components';

export const ContainerErrorMessage = styled.header`
  padding: 3.2rem 2rem;
  display: flex;
  align-items: center;
  justify-content: center;

  h1 {
    font: 700 2.4rem Archivo;
    color: var(--color-text-in-primary);
  }
`;